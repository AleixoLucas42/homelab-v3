apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: homarr-sec
  namespace: homarr 
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  refreshInterval: "5m"
  secretStoreRef:
    name: vault-cluster-backend
    kind: ClusterSecretStore
  target:
    name: homarr-sec
  dataFrom:
    - extract:
        key: "aleixohome/kubernetes/secrets/homarr"
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
