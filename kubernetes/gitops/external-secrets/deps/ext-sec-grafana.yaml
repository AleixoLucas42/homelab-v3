apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-sec
  namespace: grafana
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  refreshInterval: "5m"
  secretStoreRef:
    name: vault-cluster-backend
    kind: ClusterSecretStore
  target:
    name: grafana-sec
  dataFrom:
    - extract:
        key: "aleixohome/kubernetes/secrets/grafana"
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
