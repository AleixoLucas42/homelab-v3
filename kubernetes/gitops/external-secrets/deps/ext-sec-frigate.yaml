apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: frigate-sec
  namespace: frigate 
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  refreshInterval: "5m"
  secretStoreRef:
    name: vault-cluster-backend
    kind: ClusterSecretStore
  target:
    name: frigate-sec
  dataFrom:
    - extract:
        key: "aleixohome/kubernetes/secrets/frigate"
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
