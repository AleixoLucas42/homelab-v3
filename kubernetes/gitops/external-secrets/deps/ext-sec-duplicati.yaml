apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: duplicati-sec
  namespace: duplicati
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  refreshInterval: "5m"
  secretStoreRef:
    name: vault-cluster-backend
    kind: ClusterSecretStore
  target:
    name: duplicati-sec
  dataFrom:
    - extract:
        key: "aleixohome/kubernetes/secrets/duplicati"
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
        nullBytePolicy: Ignore
