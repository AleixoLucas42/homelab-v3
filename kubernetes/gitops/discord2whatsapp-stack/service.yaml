apiVersion: v1
kind: Service
metadata:
  name: whatsapp-api-svc
  namespace: discord2whatsapp-stack
spec:
  selector:
    app: discord2whatsapp-stack
  ports:
    - name: http
      port: 80
      targetPort: 3000
