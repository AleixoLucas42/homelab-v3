apiVersion: v1
kind: LimitRange
metadata:
  name: games-on-whales-default-limits
  namespace: games-on-whales
spec:
  limits:
  - default:
      cpu: "1"
      memory: "512Mi"
    defaultRequest:
      cpu: "100m"
      memory: "128Mi"
    type: Container
