apiVersion: v1
kind: Service
metadata:
  name: pve-exporter-svc
  namespace: pve-exporter
spec:
  selector:
    app: pve-exporter
  ports:
    - name: metrics
      port: 9221
      targetPort: 9221
  type: ClusterIP
